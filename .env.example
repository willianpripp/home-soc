# Copy to .env and fill in. .env is gitignored and never committed.
HOMESOC_DB_PASSWORD=change-me

# Where the Trivy run directories live. On the lab host this is written by the
# container_scan Ansible role in the homelab repo.
HOMESOC_SCAN_DIR=/srv/scan

# Loopback port for the database. Only needs changing if something else on the
# host already has 5433.
HOMESOC_DB_PORT=5433

# AdGuard Home's own REST API, for the DNS query log ingester.
HOMESOC_ADGUARD_URL=http://adguard.local:3000
HOMESOC_ADGUARD_USER=admin
HOMESOC_ADGUARD_PASSWORD=change-me

# How long raw dns_query rows are kept. dns_domain (the "have I seen this
# domain before" registry) is never pruned, only the per-query rows are.
HOMESOC_DNS_RETENTION_DAYS=35
